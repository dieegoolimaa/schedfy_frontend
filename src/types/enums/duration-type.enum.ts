export enum DurationType {
    /** Duração fixa (ex: corte de cabelo sempre 30 minutos) */
    FIXED = 'fixed',

    /** Duração variável (ex: consulta pode durar 30-60 minutos) */
    VARIABLE = 'variable',
}
