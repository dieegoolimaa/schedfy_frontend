export enum PackageStatus {
    /** Pacote ativo e disponível para venda */
    ACTIVE = 'active',

    /** Pacote inativo (não disponível para venda) */
    INACTIVE = 'inactive',

    /** Pacote em rascunho (ainda não publicado) */
    DRAFT = 'draft',
}
