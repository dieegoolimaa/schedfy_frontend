export enum SubscriptionStatus {
    /** Assinatura ativa */
    ACTIVE = 'active',

    /** Assinatura expirada */
    EXPIRED = 'expired',

    /** Assinatura cancelada */
    CANCELLED = 'cancelled',

    /** Assinatura pausada */
    PAUSED = 'paused',
}
