/**
 * Frontend Types - Barrel Export
 * 
 * Importar todos os tipos de um único lugar:
 * import { Region, UserRole, User, ApiResponse } from '@/types';
 */

// Enums (do backend)
export * from './enums';

// Models (interfaces de domínio)
export * from './models';

// DTOs (tipos de API)
export * from './dto';
